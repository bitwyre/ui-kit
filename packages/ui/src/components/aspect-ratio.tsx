"use client";

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

/**
  @description Displays image content within a desired ratio.
  @param [ratio] is for scaling ratio for the content.
*/
const AspectRatio = AspectRatioPrimitive.Root;

export {AspectRatio};
